<h1>All tasks list</h1>

<%= link_to  "Create a new task", tasks_new_path %>

<br>
<br>

<table class="table">
   <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Completed?</th>
    <th> Lien </th>
  </tr>
    <% @tasks.each do |task| %>
  <tr>
    <td><%= link_to task.title, task_path(task) %></td>
    <td><%= task.details  %></td>
    <td><%= task.completed %></td>
    <td><%= link_to  "Voir", task_path(task) %></td>
  </tr>
  <% end %>
</table>



  <%= link_to  "Create a new task", tasks_new_path %>
